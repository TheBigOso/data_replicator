# Connections — surface folder
The endpoints a virtual fleet replicates between.
| Spec | Wireflow |
|---|---|
| `connection.md` | `Connections Wireflow.dc.html` |

`connection.md` carries both the model (LOC-xx) and the surface (CON-xx) — merged from the former `location.md` and `connections.md`.
